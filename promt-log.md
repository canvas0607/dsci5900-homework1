promt-log
---

# 需求分析

Input:

```markdown
请解释题目要求，并且编写伪代码保存在 promote.md，后续会使用 python 3 实现。

题目要求：
创建一个名为 inventory 的列表，用来存储多个产品字典。
1. 每个产品都必须是一个字典，并包含以下键(keys)：
a. name（字符串 (string)）
b. quantity（整数 (integer)）
c. price（浮点数 (float)）
在你的 inventory 中添加 3 个产品。

2. 编写一个名为 calculate_total_value() 的函数，实现以下功能：
a. 遍历 (Loops through) 库存
b. 将每件商品的数量 × 价格相乘
c. 返回商店的总价值
打印最终的总价值。
```

Output:

```markdown
  一、题目要求解读

  将需求拆解为四个最小单元：

  ┌─────┬──────────┬───────────────────────────────────────────────────────────────────────────────────────────────────┐
  │  #  │   单元   │                                               要求                                                │
  ├─────┼──────────┼───────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 1   │ 数据结构 │ inventory 是一个 list，元素是 dict                                                                │
  ├─────┼──────────┼───────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 2   │ 数据契约 │ 每个 product 必须含三个键：name: str、quantity: int、price: float                                 │
  ├─────┼──────────┼───────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 3   │ 初始数据 │ 在 inventory 中添加 3 个产品                                                                      │
  ├─────┼──────────┼───────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 4   │ 核心函数 │ calculate_total_value(inventory)：遍历 → quantity × price 累加 → 返回 float；并在主程序中打印结果 │
  └─────┴──────────┴───────────────────────────────────────────────────────────────────────────────────────────────────┘

  ---
  现在我将伪代码写入 promote.md。
```


# 伪代码优化

Input:

```markdown
我已经写好了伪代码，请帮我检查是否符合题目要求，并输出哪些地方可以改进，但不要直接生成 python 代码
```

Output:

```markdown
  伪代码审核报告（promote.md）

  我已详细审核 promote.md 的伪代码，以下是评估：

  ✅ 优点

  1. 结构清晰：分3大段（创建库存 → 计算总价值 → 测试用例），逻辑自上而下
  2. 校验逻辑完整：类型校验（list/dict/int/float）+ 边界校验（缺失键、负值、非空字符串）
  3. 测试覆盖全面：7个测试用例覆盖正常路径（Test 1-3）和异常路径（Test 4-7）
  4. 异常类型明确：用 TypeError 区分类型问题，ValueError 区分值问题，符合 Python 惯例
  5. 错误消息可定位：提到"missing keys"等具体信息

  ⚠️ 可优化项（参考）

  1. 语言一致性：promote.md 用英文，但项目其他文件（README）用中文。README
  任务中提到"提示词换成中文"
  2. 冗余细节：每个商品的创建重复了"name/quantity/price"模板，可抽象为 add_product(name,
  qty, price)
  3. 测试数据组织：每个测试用例都重复构造 invalid_inventory，可考虑参数化（subTest）
  4. 伪代码细节：If price is not a float 字面意义下 Python 的 int 不算
  float（当前实现严格遵循），但实际业务中 int 价格也合理（如 price: 10）

  📋 审核结论

  现有伪代码逻辑上无错误，与 inventory.py
  的实现完全对应，代码可正确运行。
```
