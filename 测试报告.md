# 测试报告

| 项目 | 内容 |
|---|---|
| 课程作业 | 小组练习 1：将伪代码转换为 Python 代码的自动化 |
| 被测模块 | `inventory.py` / `calculate_total_value()` |
| 测试脚本 | `test_inventory.py` |
| 开发环境 | Python 3.13 |
| 测试日期 | 2026-07-31 |
| 用例规模 | 自动化单元测试 **21** 项 + 主程序冒烟 **1** 项 |
| 测试结论 | **通过（PASS）**：21/21 单元测试通过，主程序输出 `$5399.65` 与验算一致 |

---

## 1. 测试目的

本次测试围绕测试脚本`test_inventory.py` 展开，目标如下：

| 编号 | 测试目标 | 对应验证方式 |
|---|---|---|
| G1 | **需求符合性**：完成「3 个产品库存 → `calculate_total_value` → 打印总价值」 | 主程序冒烟 + TC-01 |
| G2 | **功能正确性**：正常路径计算结果准确（完整库存、空库存、单商品、整型价格、额外键） | TC-01～03、TC-14、TC-18～19 |
| G3 | **结构与类型契约**：非法库存结构 / 字段类型按约定抛出 `TypeError` 或 `ValueError` | TC-04～13、TC-15～17、TC-20～21 |
| G4 | **边界健壮性**：零值、空白名称、`bool` 伪装数值等边界可被正确拒绝或计算 | TC-02、TC-10、TC-12、TC-16～18 |
| G5 | **可回归性**：全部断言固化为 `unittest` | 全量 21 用例 |

---

## 2. 测试方案

### 2.1 测试范围

| 范围 | 是否覆盖 | 说明 |
|---|---|---|
| 初始库存数据构建 | 是 | Laptop / Mouse / Keyboard |
| 正常价值计算 | 是 | 完整、空、单商品、整型价、额外键、零值 |
| 结构校验 | 是 | 非 list、`None` 库存、非 dict 元素、缺键 |
| 字段类型校验 | 是 | name / quantity / price（含 bool、float 数量、字符串价格） |
| 数值边界 | 是 | 负数量、负价格、零值、空白名称 |
| UI / 持久化 / 并发 / 网络安全 | 否 | 作业不要求 |

### 2.2 测试策略

1. **自动化单元测试**：`unittest` 驱动，黑盒断言返回值与异常类型/消息。
2. **主程序冒烟**：直接运行 `inventory.py`，核对打印格式与总额。
3. **白盒复核**：对照实现分支，确认 21 个用例覆盖主要校验路径。

### 2.3 环境与执行命令

```bash
cd dsci5900-homework1
uv run --project .. python -m unittest -v test_inventory.py
uv run --project .. python inventory.py
```

### 2.4 通过准则

| 准则 | 判定标准 |
|---|---|
| 单元测试 | 21 个用例全部 `ok`，汇总 `OK` |
| 主程序 | 输出 `Total inventory value: $5399.65` |
| 异常用例 | 抛出约定异常类型，且正则匹配关键错误信息 |
| 正常/边界用例 | 数值用 `assertEqual` / `assertAlmostEqual(..., places=2)` 判定 |

### 2.5 用例分类统计

| 类型 | 数量 | 用例编号 |
|---|---:|---|
| 正常（Happy Path） | 4 | TC-01、TC-03、TC-14、TC-19 |
| 边界（Boundary） | 2 | TC-02（空库存）、TC-18（零数量/零价格） |
| 异常（Negative） | 15 | TC-04～TC-13、TC-15～TC-17、TC-20～TC-21 |
| **合计** | **21** | 见第 3 节完整清单 |

---

## 3. 测试用例清单（目标 / 预期 / 结果）

以下清单与 `test_inventory.py` **一一对应**（按用例编号排序，非 unittest 字母序）。

| 编号 | 方法名 | 类型 | 测试目标 | 输入要点 | 预期结果 | 实际结果 | 状态 |
|---|---|---|---|---|---|---|---|
| TC-01 | `test_full_inventory` | 正常 | 验证预置三商品总价值正确（G1/G2） | 模块级 `inventory`（Laptop/Mouse/Keyboard） | `≈ 5399.65`（2 位小数） | `5399.65` | **PASS** |
| TC-02 | `test_empty_inventory` | 边界 | 空库存累加起点为 0（G2/G4） | `[]` | `0.0` | `0.0` | **PASS** |
| TC-03 | `test_single_product` | 正常 | 单商品 `qty×price` 正确（G2） | Notebook×4 @ 2.50 | `≈ 10.00` | `10.00` | **PASS** |
| TC-04 | `test_missing_key` | 异常 | 缺 `price` 键时拒绝（G3） | `{name, quantity}` | `ValueError`，消息含 `missing required keys: price` | 抛出匹配异常 | **PASS** |
| TC-05 | `test_incorrect_quantity_type` | 异常 | 字符串数量被拒绝（G3） | `quantity="four"` | `TypeError`，消息含 `Quantity` | 抛出匹配异常 | **PASS** |
| TC-06 | `test_negative_quantity` | 异常 | 负数量被拒绝（G3/G4） | `quantity=-4` | `ValueError`，消息含 `cannot be negative` | 抛出匹配异常 | **PASS** |
| TC-07 | `test_product_is_not_a_dictionary` | 异常 | 非 dict 元素被拒绝（G3） | `["Notebook"]` | `TypeError`，消息含 `must be a dictionary` | 抛出匹配异常 | **PASS** |
| TC-08 | `test_inventory_is_not_a_list` | 异常 | 顶层非 list 被拒绝（G3） | 传入 dict | `TypeError`，消息含 `Inventory must be a list` | 抛出匹配异常 | **PASS** |
| TC-09 | `test_inventory_is_none` | 异常 | `None` 库存被拒绝（G3） | `None` | `TypeError`，消息含 `Inventory must be a list` | 抛出匹配异常 | **PASS** |
| TC-10 | `test_empty_product_name` | 异常 | 空白名称被拒绝（G3/G4） | `name="  "` | `ValueError`，消息含 `non-empty string name` | 抛出匹配异常 | **PASS** |
| TC-11 | `test_product_name_is_not_a_string` | 异常 | 非字符串名称被拒绝（G3） | `name=123` | `ValueError`，消息含 `non-empty string name` | 抛出匹配异常 | **PASS** |
| TC-12 | `test_boolean_quantity` | 异常 | `bool` 不可充当数量（G3/G4） | `quantity=True` | `TypeError`，消息含 `Quantity` | 抛出匹配异常 | **PASS** |
| TC-13 | `test_float_quantity` | 异常 | 浮点数量被拒绝（G3） | `quantity=1.5` | `TypeError`，消息含 `Quantity` | 抛出匹配异常 | **PASS** |
| TC-14 | `test_integer_price_is_accepted` | 正常 | 整型价格可计算（G2） | Cable×2 @ `10`（int） | `≈ 20.0` | `20.0` | **PASS** |
| TC-15 | `test_incorrect_price_type` | 异常 | 字符串价格被拒绝（G3） | `price="2.50"` | `TypeError`，消息含 `Price` | 抛出匹配异常 | **PASS** |
| TC-16 | `test_boolean_price` | 异常 | `bool` 不可充当价格（G3/G4） | `price=True` | `TypeError`，消息含 `Price` | 抛出匹配异常 | **PASS** |
| TC-17 | `test_negative_price` | 异常 | 负价格被拒绝（G3/G4） | `price=-2.50` | `ValueError`，消息含 `cannot be negative` | 抛出匹配异常 | **PASS** |
| TC-18 | `test_zero_quantity_and_price` | 边界 | 零数量/零价格合法且小计为 0（G2/G4） | `quantity=0, price=0.0` | `0.0` | `0.0` | **PASS** |
| TC-19 | `test_extra_keys_are_ignored` | 正常 | 额外字段不影响计算（G2） | 含 `sku` 的商品，2×3.5 | `≈ 7.0` | `7.0` | **PASS** |
| TC-20 | `test_missing_name_key` | 异常 | 缺 `name` 键时拒绝（G3） | `{quantity, price}` | `ValueError`，消息含 `missing required keys: name` | 抛出匹配异常 | **PASS** |
| TC-21 | `test_product_is_none` | 异常 | 元素为 `None` 被拒绝（G3） | `[None]` | `TypeError`，消息含 `must be a dictionary` | 抛出匹配异常 | **PASS** |

### 3.1 主程序冒烟（非 unittest）

| 编号 | 目标 | 操作 | 预期 | 实际 | 状态 |
|---|---|---|---|---|---|
| SM-01 | 验证可执行入口打印格式与总额（G1） | `uv run --project .. python inventory.py` | `Total inventory value: $5399.65` | 输出完全一致 | **PASS** |

手工验算：`5×899.99 + 20×19.99 + 10×49.99 = 4499.95 + 399.80 + 499.90 = 5399.65`。

---

## 4. 测试执行与结果汇总

### 4.1 执行记录

```text
Ran 21 tests in 0.000s
OK
```

完整 verbose 输出中全部用例均为 `... ok`（执行顺序为方法名字母序，与上表 TC 编号无关）。

### 4.2 结果汇总表

| 指标 | 数值 |
|---|---:|
| 计划用例数 | 21 |
| 执行用例数 | 21 |
| 通过（PASS） | 21 |
| 失败（FAIL） | 0 |
| 错误（ERROR） | 0 |
| 跳过（SKIP） | 0 |
| 通过率 | **100%** |
| 主程序冒烟 | **PASS** |

### 4.3 按目标达成情况

| 目标 | 关联用例 | 达成情况 |
|---|---|---|
| G1 需求符合性 | TC-01、SM-01 | 达成 |
| G2 功能正确性 | TC-01～03、TC-14、TC-18～19 | 达成 |
| G3 结构与类型契约 | TC-04～13、TC-15～17、TC-20～21 | 达成 |
| G4 边界健壮性 | TC-02、TC-10、TC-12、TC-16～18 | 达成 |
| G5 可回归性 | 全部 21 项 | 达成（`uv` + `unittest` 可复现） |

### 4.4 按类型结果分布

| 类型 | 用例数 | 通过 | 失败 |
|---|---:|---:|---:|
| 正常 | 4 | 4 | 0 |
| 边界 | 2 | 2 | 0 |
| 异常 | 15 | 15 | 0 |
| 冒烟（SM-01） | 1 | 1 | 0 |

---

## 5. 代码复核与漏洞评估

### 5.1 需求符合性复核

| PDF / 伪代码要求 | 实现位置 | 结论 |
|---|---|---|
| `inventory` 为 list，元素为含 name/quantity/price 的 dict | `inventory.py` 数据段 | 满足 |
| 添加 3 个产品 | Laptop / Mouse / Keyboard | 满足 |
| `calculate_total_value` 遍历并累加 quantity×price | 函数主体 | 满足 |
| 打印总价值 | `__main__` | 满足，`$5399.65` |
| 类型/缺键/负值校验 | 函数前置校验 | 满足；价格已接受 `int`/`float`（拒绝 `bool`） |

### 5.2 健壮性发现

| 严重级别 | 位置 | 发现 | 状态 |
|---|---|---|---|
| 已修复 | 价格类型校验 | 原仅 `float`，现接受 `int`/`float` 并拒 `bool` | 已由 TC-14/15/16 覆盖 |
| 低 | 金额使用 `float` | IEEE754 精度限制 | 接受；比较用 `places=2` |
| 信息 | 模块级全局 `inventory` | 可变全局状态 | 风险低；多数用例使用独立列表 |
| 无 | 注入 / XSS / 权限类 | 无外部不可信输入 | 不适用 |

---

## 6. 结果评估与验收

### 6.1 总体评价

- **清单完整性**：报告用例与 `test_inventory.py` 方法 **21/21 对齐**，每条均含目标、预期与实际结果。
- **目标达成**：G1～G5 全部达成。
- **正确性 / 健壮性**：正常、边界、异常三类路径均通过。

### 6.2 残余风险（不阻塞验收）

1. 浮点金额在极端累加下可能出现显示外误差；生产环境可改用 `decimal.Decimal`。
2. 全局 `inventory` 不利于大型项目隔离；本作业规模可接受。

### 6.3 验收判定

| 检查项 | 结果 |
|---|---|
| `unittest` 21/21 OK | 通过 |
| 主程序输出 `$5399.65` | 通过 |
| 用例清单与代码一致 | 通过 |
| 测试目标 G1～G5 | 全部达成 |

**最终判定：通过（PASS）。**

---

## 7. 附录

### 7.1 被测初始数据

| name | quantity | price | 小计 |
|---|---:|---:|---:|
| Laptop | 5 | 899.99 | 4499.95 |
| Mouse | 20 | 19.99 | 399.80 |
| Keyboard | 10 | 49.99 | 499.90 |
| **合计** |  |  | **5399.65** |

### 7.2 相关文件

- `inventory.py` — 实现
- `test_inventory.py` — 自动化测试（21 用例）
- `promote.md` — 伪代码与测试伪代码
- `promt-log.md` — Prompt 日志
- `README.md` — 运行说明
- `测试报告.md` — 本报告
